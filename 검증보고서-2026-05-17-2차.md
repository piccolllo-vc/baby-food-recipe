# LittleMates 2차 검증 보고서 — 탭별 기능 코드 분석

**검증일:** 2026-05-17
**검증자:** 장그래 (OpenClaw)
**방법:** 코드 정적 분석 (브라우저 자동화 불가 → 소스 코드 직접 분석)

---

## 🔍 발견된 심각한 버그

### 🚨 결함 함수·변수 목록 (코드에서 호출되지만 정의 안 됨)

| 호출되는 곳 | 상태 | 설명 |
|------------|------|------|
| `loadRecipes()` (line 610) | ❌ **정의 없음** | `RECIPES` 변수에 데이터载入 안 됨 |
| `RECIPES` (lines 496, 500, 501, 521, 527, 528, 557, 558, 561) | ❌ **정의 없음** | 전역 레시피 배열 없음 → `renderRecipes()` 실행 불가 |
| `filterRecipes('6m')` (lines 59~62) | ❌ **정의 없음** | 연령대 퀵 링크 버튼 클릭 시 함수不存在 |
| `getAllowedAges(babyMonths)` (lines 500, 557) | ❌ **정의 없음** | 아기 연령대별 허용 레시피 필터 불가 |
| `currentAgeFilter` (line 561) | ❌ **정의 없음** | 연령대 필터 상태 추적 불가 |
| `openBabyModal()` (line 126) | ❌ **정의 없음** | 아기 정보 등록 Modal 열기 불가 |
| `closeBabyModal()` (line 174) | ❌ **정의 없음** | 아기 정보 Modal 닫기 불가 |
| `saveBabyProfile()` (line 192) | ❌ **정의 없음** | 아기 정보 저장 불가 |
| `babyMonths` (global, lines 455, 462, 500, 557) | ❌ **정의 없음** (global 없음) | 개월수 계산 결과 저장 변수 없음 |

---

## 각 탭별 상세 분석

### 🏠 탭: 홈 (Home)

| 요소 | 코드 상태 | 비고 |
|------|----------|------|
| Hero 섹션 | ✅ 정상 | 렌더링 ok |
| 연령대 퀵 링크 (6개월~ / 7-9개월 / 10-12개월 / 12개월+) | ❌ `filterRecipes()` 정의 없음 | 클릭 시 JavaScript 에러 |
| 추천 레시피 (homeRecipeList) | ⚠️ `RECIPES` undefined → 데이터 못 불러옴 | `renderHomeRecipes()` 실행 불가 |
| 빠른 작업 — 냉장고 / 아기정보 버튼 | ✅ `switchTab()` 정상 | 탭 전환 ok |

**영향:** 홈 화면의 **연령대 퀵 링크 버튼**과 **추천 레시피**가 동작 안 함.

---

### 🧊 탭: 냉장고 (Fridge)

| 요소 | 코드 상태 | 비고 |
|------|----------|------|
| 냉장고 Selector + 새 냉장고 생성 | ✅ `loadFridges()`, `openNewFridgeModal()` 정상 | |
| 재료 추가 Modal (분류 / 재료명 / 수량 / 유통기한) | ✅ `populateCategorySelect()`, `onCategoryChange()`, `onNameSelectChange()` 정상 | |
| `addFridgeItem()` | ✅ 정상 | Supabase fridge_items insert |
| `deleteFridgeItem()` | ✅ 정상 | Supabase delete |
| 유통기한 임박 경고 | ✅ `expiryWarning` div 생성逻辑 ok | |
| 알림 설정 버튼 | ✅ `requestExpiryNotif()` 정상 | |

**영향:** 냉장고 탭은 **전체적으로 정상**运作.

---

### 🍳 탭: 레시피 (Recipes)

| 요소 | 코드 상태 | 비고 |
|------|----------|------|
| 레시피 검색창 (`recipeSearchInput`) | ⚠️ `handleRecipeSearch()` 존재하지만 `RECIPES` undefined → 검색 불가 | |
| 알레르기 필터 버튼 | ⚠️ `toggleAllergyFilter()` 존재하지만 `RECIPES` undefined | |
| 연령대 필터 (`currentAgeFilter`) | ❌ `currentAgeFilter` 변수 정의 없음 | 필터 버튼 생성 안 됨 |
| 레시피 카드 목록 (`recipeList`) | ❌ `RECIPES` undefined → 빈 화면 또는 에러 | |
| 좋아요 버튼 (♥) | ✅ `toggleLike()` 존재 | 로그인한 사용자만 동작 |
| 레시피 상세 Modal | ✅ `openRecipeModal()`, `closeRecipeModal()` 정상 | |

**영향:** 레시피 탭에서 **레시피 목록 자체가 안 뜰 수 있음** (`RECIPES`가 undefined).

---

### 👶 탭: 아기정보 (Baby)

| 요소 | 코드 상태 | 비고 |
|------|----------|------|
| 아기 정보 표시 | ✅ `loadBabyProfile()` 정상 | baby_profiles에서 데이터 불러와서 렌더링 |
| 아기 정보 등록 버튼 | ❌ `openBabyModal()` 정의 없음 | 버튼 클릭 시 JavaScript 에러 |
| Modal 닫기 | ❌ `closeBabyModal()` 정의 없음 | Modal 자체가 없음 |
| 저장 버튼 | ❌ `saveBabyProfile()` 정의 없음 | Modal 자체가 없음 |

**영향:** 아기 정보 **등록이 완전히 불가능**. 기존 데이터 조회만 가능.

---

## Root Cause

`loadRecipes()` 함수 자체가 없어서 `RECIPES` 전역 배열에 데이터가 저장되지 않음.
`data/recipes.json`을 `fetch()`로 불러오는 로직이 `initAuth()` 이후에 실행되어야 하는데, **해당 함수가 존재하지 않음**.

대부분의 연령대 필터, 아기 정보 등록 Modal, 홈 추천 레시피가 이 `RECIPES`와 연관된 함수에 의존하고 있음.

---

## 결론

| 우선순위 | 항목 | 상태 |
|----------|------|------|
| **치명적** | `loadRecipes()` 함수 없음 → 레시피 목록 안 뜸 | ❌ |
| **치명적** | `RECIPES` 변수 없음 → 전체 레시피 기능 무효 | ❌ |
| **치명적** | 아기 정보 등록 Modal 없음 → 아기 정보 입력 불가 | ❌ |
| **높음** | `filterRecipes()` 없음 → 홈 연령대 퀵 링크 동작 안 함 | ❌ |
| **높음** | `getAllowedAges()`, `currentAgeFilter` 없음 → 연령대 필터 무효 | ❌ |

**모든 레시피 관련 기능 + 아기 정보 등록이 동작하지 않는 상태.**

코드를 직접 수정하려면 `loadRecipes()` 추가 + Modal 3개 함수 정의 + `RECIPES`/`babyMonths`/`currentAgeFilter` 전역 변수 정의가 필요합니다.

---

*최종 업데이트: 2026-05-17*
*검증자: 장그래 (OpenClaw)*
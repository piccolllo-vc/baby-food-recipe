# LittleMates 최종 수익화 시스템 설계

> 업데이트: 2026-05-20
> 버전: 2.0 (커뮤니티 + Subscription)

---

## 1. 플랜 구조

### Free (무료)
| 기능 | 제한 |
|------|------|
| 레시피 열람 | 월령별 3개만 |
| 좋아요 | ❌ |
| 레시피 올리기 | ❌ |
| 알레르기 필터 | ❌ |
| 즐겨찾기 | ❌ |
| 유통기한 알림 | ❌ |

### Premium (월 4,900원 / 연 49,000원)
| 기능 | 제한 |
|------|------|
| 레시피 열람 | **무제한** |
| 좋아요 | ✅ |
| 레시피 올리기 | ✅ (하루 1개) |
| 알레르기 필터 | ✅ |
| 즐겨찵기 | ✅ |
| 유통기한 알림 | ✅ |
| 커뮤니티 탭 접근 | ✅ |

---

## 2. 커뮤니티 기능

### 구조
```
[탭: 커뮤니티]
├── 🔥 이번 주 인기 레시피 (좋아요 TOP 10)
├── 🆕 최신 레시피 (시간순)
├── 👶 내 레시피 (내가 올린 것)
└── ❤️ 좋아요한 레시피
```

### 사용자 레시피 업로드
```javascript
// Supabase Storage에 이미지 + 메타데이터
{
  id: uuid,
  user_id: auth.users.id,
  title: "우리 아기 첫 단호박 미음",
  description: "6개월 된 아기에게 먹인 첫 이유식이에요",
  image_url: supabase/storage/uploads/xxx.jpg,
  age_months: 6,
  ingredients: ["단호박 50g", "재유 30g", "물 100ml"],
  method: ["...", "..."],
  likes_count: 0,
  views_count: 0,
  created_at: timestamp
}
```

### 좋아요 시스템
```sql
CREATE TABLE recipe_likes (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  recipe_id TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, recipe_id)
);
```

### 중계 테이블 (기존 유저 likes와 통합)
```javascript
// 레시피 종류: 'official' (기본) | 'community' (사용자 작성)
{
  id: "official_1",  // prefix로 구분
  type: "official",
  ...
}
{
  id: "comm_xxx",
  type: "community",
  user_id: "...",
  likes_count: 5,
  ...
}
```

---

## 3. 결제 시스템

### PG사 선택
| PG | 수수료 | 특징 |
|-----|--------|------|
| **Stripe** | 3.4% + ₩50 |国际건대, API 간단 |
| **IamPort** | 3.4% + ₩50 |국내 PG사 통합 |
| **Toss Payments** | 3.4% + ₩50 |사용자 친숙 |

**추천: Stripe** (Supabase Billing과 연동 쉬움)

### Subscription 플랜
```javascript
// Stripe Customer Portal 사용
const session = await stripe.billingPortal.sessions.create({
  customer: customerId,
  return_url: 'https://yourapp.com/settings'
});

// Checkout
const checkout = await stripe.checkout.sessions.create({
  mode: 'subscription',
  line_items: [{ price: 'price_xxx', quantity: 1 }],
  success_url: '...',
  cancel_url: '...'
});
```

### Supabase 플랜 테이블
```sql
CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) NOT NULL,
  plan TEXT CHECK (plan IN ('free', 'premium')),
  status TEXT CHECK (status IN ('active', 'cancelled', 'past_due')),
  stripe_customer_id TEXT,
  stripe_subscription_id TEXT,
  current_period_end TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);

ALTER TABLE subscriptions ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own subscription" ON subscriptions
  FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can update own subscription" ON subscriptions
  FOR UPDATE USING (auth.uid() = user_id);
```

### 플랜 확인 로직
```javascript
async function isPremium(userId) {
  const { data } = await sb.from('subscriptions')
    .select('status, current_period_end')
    .eq('user_id', userId)
    .single();
  
  return data?.status === 'active' && 
         new Date(data.current_period_end) > new Date();
}

// 사용 시
if (!await isPremium(userId)) {
  showUpgradeModal();
  return;
}
```

---

## 4. 무료 배포 비용

### 실제 발생하는 비용
| 항목 | 비용 | 비고 |
|------|------|------|
| **도메인** | ~$12/年 | 선택 (localhost 가능) |
| **호스팅 (Vercel)** | **무료** | personal 사용시 무제한 |
| **Supabase** | **무료** | 500MB DB, 1GB Storage |
| **이미지 저장 (Supabase Storage)** | **무료** | 1GB 무료 |
| **PG 수수료** | 3.4% | 결제時만 발생 |

### 배포 파이프라인
```
GitHub Push (main branch)
    ↓
Vercel 자동 배포 (CI/CD)
    ↓
HTTPS + CDN 자동 적용
    ↓
supabase.com/api → Supabase DB
```

### 도메인 연결 (선택)
- Vercel: 무료 서브도메인 (`littlemates.vercel.app`)
- 커스텀 도메인: `.com` 약 $12/年

---

## 5. 예상 수익 모델

### 유료 전환율 가정
- 월 방문자: 1,000명
- 무료 → Premium 전환: **3%** (30명)
- 월 수익: 30 × ₩4,900 = **₩147,000/月**
- 연 수익: **₩1,764,000/年** (PG 수수료 제외)

### 성장 시나리오
| 월 방문자 | 전환율 | 월 수익 |
|-----------|--------|--------|
| 1,000 | 3% | ₩147,000 |
| 5,000 | 3% | ₩735,000 |
| 10,000 | 3% | ₩1,470,000 |

---

## 6. 구현 우선순위

| 순위 | 단계 | 내용 | 소요시간 |
|------|------|------|----------|
| 1 | 플랜 게이팅 | Free/Premium 분기 처리 | 2-3시간 |
| 2 | Subscriptions 테이블 | Supabase 마이그레이션 | 1시간 |
| 3 | 커뮤니티 탭 | UGC 리스트 + 좋아요 | 4-6시간 |
| 4 | 레시피 업로드 | 이미지 업로드 + 저장 | 3-4시간 |
| 5 | Stripe 연동 | Checkout + Portal | 3-4시간 |
| 6 | 배포 | Vercel + 도메인 | 1시간 |

**총 예상 개발시간: 14-20시간**

---

## 7. 즉시 시작 가능 (비용 0원)

- 지금 바로 개발 시작 가능
- 도메인 없이 `localhost`로 테스트
- Vercel 무료 플랜으로 배포
- Supabase 무료 플랜으로 운영
- PG 연동后才会有 비용 발생 (거래時 3.4%)

---

*최종 업데이트: 2026-05-20*
*GitHub: https://github.com/piccolllo-vc/baby-food-recipe*
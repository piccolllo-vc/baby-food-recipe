# LittleMates 프로젝트 상태

> 베이비 푸드 웹앱 프로젝트 (원래 Nurture/너처로 시작)

## 기본 정보

| 항목           | 내용                                               |
| ------------ | ------------------------------------------------ |
| **프로젝트명**    | LittleMates                                      |
| **목적**       | 아기 이유식 레시피 추천 + 냉장고 관리                           |
| **플랫폼**      | HTML + Tailwind CSS + JS (PWA 예정)                |
| **현재 경로**    | `/home/jssh/Obsidian-jssh/projects/LittleMates/` |
| **마지막 업데이트** | 2026-05-15                                       |

---

## 🚀 Supabase 프로젝트

| 항목 | 내용 |
|------|------|
| **URL** | https://dlphziojfyzkhsftgdkm.supabase.co |
| **Ref** | dlphziojfyzkhsftgdkm |
| **생성일** | 2026-05-05 |

---

## 진행 상황

### ✅ 완료

| 단계 | 내용 | 완료일 |
|------|------|--------|
| TS-1 | Supabase 프로젝트 생성 | 2026-05-05 |
| TS-2 | Supabase Auth 설정 (Google OAuth) | 2026-05-05 |
| TS-3 | Supabase JS SDK 연동 + Google OAuth 로그인 | 2026-05-05 |
| TS-4 | config.js 환경변수 분리 | 2026-05-07 |
| TS-5 | localStorage → Supabase 마이그레이션 코드 작성 | 2026-05-07 |

---

## 🔧 버그 수정 기록 (2026-05-05)
### TS-6: recipes.json 연동 + 레시피 상세 Modal ✅ (2026-05-11)
- `recipes.json` 6개 레시피 동적 로드
- 레시피 카드 클릭 시 상세 Modal 표시 (재료, 만드는 법, 영양정보)
- 알레르기 정보 표시 (fish, soy 등)
- 기존 하드코딩 샘플(한글 깨짐) 제거

### TS-7: Supabase 테이블 RLS 확인 ✅ (2026-05-11)
- Supabase MCP 서버로 직접 테이블/RLS 확인
- baby_profiles, fridges, fridge_items 모두 RLS 활성화 확인
- user_id → auth.users.id 외래키 OK
- fridge_id → fridges.id ON DELETE CASCADE OK



### TS-8: PWA Service Worker + manifest.json ✅ (2026-05-11)
- `manifest.json` 생성 (standalone display, theme-color, SVG icon)
- `sw.js` 생성 (cache-first → network-fallback 전략)
- PWA 아이콘: inline SVG data URL
- 오프라인에서도 기본 shell + 캐시된 recipes.json 로드 가능

### TS-9: ingredients.json → fridge modal 연동 ✅ (2026-05-11)
- 페이지 로드 시 `loadIngredients()`로 `ingredients.json` fetch → `INGREDIENTS` 전역 객체 저장
- 냉장고 Modal: 기존 "재료명 input + 분류 select" → **분류 select + 재료명 select 2단 구조**
- 분류 선택 시 `onCategoryChange()`로 해당 카테고리 재료 리스트 동적 생성
- 재료명 select에 "✏️ 직접 입력" 옵션 추가 → 고르면 input[type=text]로 fallback
- `addFridgeItem()` 로직 그대로 유지 (name은 string으로 정상 전달)
- 기존 auth/Supabase CRUD 코드 미손촉

### TS-10: 아기 연령대별 레시피 필터 기능 ✅ (2026-05-11)
- 레시피 탭에 연령대 필터 버튼 UI 추가 (`전체 / 6개월~ / 7-9개월 / 10-12개월 / 12개월+`)
- `currentAgeFilter` 전역 변수: 현재 선택된 연령대 필터 상태
- `babyMonths` 전역 변수: 아기 생년월일로부터 계산된 개월수
- `loadBabyProfile()` 호출 시 개월수 자동 계산 및 저장
- 아기 연령에 맞는 레시피 자동自强 필터 (예: 6개월 아기 → `6m`만 표시)
- 필터 버튼 클릭 → `filterRecipes()` → `renderRecipes(filtered)` 호출
- 아기 정보 없으면 전체 레시피 표시

### 1. const 변수 재할당 에러 (해결 ✅)
- **문제:** `const RECIPES`, `const INGREDIENT_CATEGORIES`를 loadData()에서 덮어쓰려고 했음
- **수정:** `const` → `let`으로 변경
- **파일:** index.html line 671, 793

### 2. supabase 변수 숨김 (해결 ✅)
- **문제:** `const supabase = supabase.createClient(...)`에서 자기 자신 재할당
- **수정:** 변수명 `sb`로 변경 (line 1141)
- **영향:** 모든 `sb.auth.*`, `sb.from(*)` 호출은 정상 작동 확인

### 3. Google OAuth redirectTo 오류 (해결 ✅)
- **문제:** `redirectTo: window.location.origin + '/auth/callback'` → "requested path is invalid"
- **수정:** redirectTo 옵션 제거 → Supabase 기본 callback 사용
- **필요 설정:**
  - Google Cloud Console: Redirect URI 등록 (`http://localhost:8082`)
  - Supabase Dashboard: Redirect URLs에 `http://localhost:8082` 등록

---

## ✨ 신규 기능 (2026-05-05)

### 다크 모드 구현 ✅
- 라이트/다크 테마 전환 토글 (마이페이지 → 설정)
- localStorage로 테마 저장 (새로고침해도 유지)
- CSS 변수 기반 테마 시스템

### Navbar 로그인 버튼 동적 변경 ✅
- 로그인 전: `로그인` 버튼 표시
- 로그인 후: 사용자 이름 + 아이콘으로 교체 (클릭 시 마이페이지 이동)
- updateAuthUI() 함수에서 동기 처리

---

## 파일 구조

```
LittleMates/
├── index.html          # 메인 앱 (현재 ~57KB)
├── config.js           # Supabase 환경변수 (2026-05-07 추가)
└── data/
    ├── baby-info-template.json   # 아기 정보 템플릿
    ├── ingredients.json          # 재료 데이터
    └── recipes.json             # 레시피 데이터 (6개)
```

---

## 기술 스택

| 항목 | 내용 |
|------|------|
| **Frontend** | HTML5 + Tailwind CSS + Vanilla JavaScript |
| **Backend** | Supabase (PostgreSQL + Auth + Realtime) |
| **Auth** | Supabase Auth (Google OAuth) |
| **Icons** | Google Material Symbols |
| **Fonts** | Plus Jakarta Sans, Be Vietnam Pro |
| **Theme** | CSS Custom Properties (Light/Dark) |

---

## Supabase Auth 설정 완료 내용

### Google OAuth (작동 확인 ✅)
- Supabase Dashboard → Authentication → Providers → Google
- Google Cloud Console에서 OAuth Client 등록
- Redirect URLs 양쪽 모두 등록 완료

### 사용된 설정
```javascript
const sb = supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

async function signInWithGoogle() {
  const { data, error } = await sb.auth.signInWithOAuth({
    provider: 'google'
  });
}
```

---

## Supabase 스키마 (예상)

```
users          -- Auth.users 연동 (자동)
profiles       -- 사용자 프로필 (user_id, name, created_at)
fridges        -- 냉장고 (id, user_id, name, created_at)
fridge_items   -- 냉장고 내 재료 (id, fridge_id, name, category, quantity, expiry_date)
baby_info      -- 아기 정보 (id, user_id, name, birth_date, allergies)
recipes        -- 레시피 (id, title, description, age_months, image_url, steps)
recipe_ingredients -- 레시피 재료 (id, recipe_id, name, amount)
```

---

## 진행 규칙

1. **기술 스택(TS-1 ~ TS-5) 완료 후에야 다음 단계 진행**
2. 각 단계 완료 시 wiki 상태 문서 업데이트
3. GitHub에 주기적 푸시

---

## ✅ Supabase 테이블 및 RLS 정책 생성 완료 (TS-12, 2026-05-14)

- `baby_profiles`, `fridges`, `fridge_items` 테이블 생성 완료
- RLS 정책 모두 적용 완료
- `fridge_items`의 경우 `fridges.user_id` 서브쿼리로 RLS 처리
- Supabase CLI 마이그레이션으로 관리 중

```sql
-- 아기 프로필
CREATE TABLE baby_profiles (
  id UUID DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  name TEXT,
  birth_date DATE,
  allergies TEXT[],
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  PRIMARY KEY (id)
);

-- 냉장고
CREATE TABLE fridges (
  id UUID DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  name TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  PRIMARY KEY (id)
);

-- 냉장고 재료
CREATE TABLE fridge_items (
  id UUID DEFAULT gen_random_uuid(),
  fridge_id UUID REFERENCES fridges(id) ON DELETE CASCADE,
  name TEXT,
  category TEXT,
  quantity TEXT,
  expiry_date DATE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  PRIMARY KEY (id)
);

-- RLS 정책 (Row Level Security)
ALTER TABLE baby_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE fridges ENABLE ROW LEVEL SECURITY;
ALTER TABLE fridge_items ENABLE ROW LEVEL SECURITY;

-- 사용자 본인만 접근 가능
CREATE POLICY "Users can manage own baby profile" ON baby_profiles FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users can manage own fridges" ON fridges FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users can manage own fridge items" ON fridge_items FOR ALL USING (auth.uid() = user_id);
```

---

## 완료된 다음 단계 (TS-4 → TS-5)

### TS-4: config.js 환경변수 분리 ✅
- `config.js` 파일 생성
- Supabase URL과 Anon Key를 `CONFIG` 객체로 분리
- index.html에서 `config.js` 참조하도록 수정

### TS-5: localStorage → Supabase 마이그레이션 ✅
- Supabase 테이블 구조 정의 완료
- localStorage 데이터 → Supabase 저장 로직 작성
- 유저별 데이터 동기화 코드 구현

---

## ✅ 로컬 앱 기본 응답 확인 완료 (TS-12, 2026-05-15)

- `python3 -m http.server 8082`로 로컬 서버 실행 확인
- `http://127.0.0.1:8082/` 응답 `200 OK` 확인
- `manifest.json`, `data/recipes.json`, `data/ingredients.json` JSON 파싱 확인
- `index.html`에서 `config.js`, `manifest.json`, `sw.js`, `data/recipes.json`, `data/ingredients.json` 참조 확인

## ✅ 화면 기본 흐름 확인 및 저장 오류 처리 보강 (TS-13, 2026-05-16 ✅)

- 로컬 서버(`python3 -m http.server 8082`) 정상 동작 확인
- Safari에서 앱 화면 열기, 탭 이동, 레시피 필터 동작 확인
- 냉장고 재료 추가 코드에서 Supabase 응답 처리 보강:
  - 냉장고 조회 실패
  - 냉장고 생성 실패
  - 재료 추가 실패
  - 재료 삭제 실패
- **✅ 대표님 Google 계정으로 로그인 → Supabase 저장·조회 확인 완료 (2026-05-16)**
  - baby_profiles: 1행 저장 확인
  - fridges: 1행 저장 확인
  - fridge_items: 1행 저장 확인
  - RLS 정책 정상 작동 (본인 데이터만 접근 가능)

## 관련 문서

- [[Nurture-검증체크리스트]] - 앱 검증 체크리스트
- [[baby-food-평가보고서-2026-04-25]] - 기존 앱 평가 보고서

### TS-11: GitHub 저장소 생성 + 푸시 ✅ (2026-05-11)
- 저장소명: `baby-food-recipe`
- URL: https://github.com/piccolllo-vc/baby-food-recipe
- 첫 커밋: "Initial commit: LittleMates baby food recipe app with Supabase, PWA"
- 푸시 완료: main 브랜치远程推送 성공

---

### TS-14: 홈탭 추가 ✅ (2026-05-16)
- 초기 스티치 코드에서 홈탭(Hero 섹션 + 연령대 퀵 링크 + 추천 레시피 + 빠른 작업) 발견
- 탭 네비게이션에 `🏠 홈` 버튼 추가 (기본 선택 탭으로 설정)
- `tab-fridge` 기존 `tab-content` → `tab-content hidden`으로 변경 (홈이 기본)
- `switchTab('home')` 호출 시 `loadHomeRecipes()` 실행
- `loadHomeRecipes()` 함수 추가: 상위 4개 레시피 카드를 홈에 표시

---

*최종 업데이트: 2026-05-16*
*TS-12 ✅ Supabase 테이블 + RLS 정책 생성 완료*
*TS-12 ✅ 로컬 앱 기본 응답 확인 완료*
*TS-13 ✅ 로그인 후 Supabase 저장·조회 확인 완료*
*TS-14 ✅ 홈탭 추가 완료 (2026-05-16)*
*TS-4, TS-5, TS-11 ✅ 완료*

---

## 📋 해야할 일 (To-Do)

### TS-15: 아기 연령대 기반 홈 개인화 ✅ (2026-05-16)
- `loadHomeRecipes()`에서 `getAllowedAges(babyMonths)` 활용
- 아기 월령에 맞는 레시피 우선 표시 (4개)
- 아기 정보 없으면 전체 레시피 중 4개 표시 (기존 동작 유지)

### TS-16: 유통기한 임박 재료 푸시 알림 ✅ (2026-05-16)
- PWA Notification API 활용
- 냉장고 조회 후 유통기한 3일 이하 재료 → 알림 발송
- 알림 권한 요청 + 브라우저 지원 여부 확인
- 알림 설정/해제 토글 버튼 UI 추가
- localStorage로 알림 설정 상태 관리

### TS-17: 레시피 북마크/좋아요 기능 ✅ (2026-05-16)
- `user_likes(user_id, recipe_id)` 테이블 추가
- 레시피 카드에 ♥ 버튼 추가
- 로그인 사용자만 좋아요 가능
- 좋아요 상태 Supabase에 저장
- 렌더링 시 userLikes 객체 참조하여 하트 색상 변경

### TS-18: 레시피 검색 기능 고도화 ✅ (2026-05-16)
- 검색어 입력창 추가 (레시피명, 설명, 재료명 검색)
- 알레르기 제외 필터 버튼 (fish, soy, milk, egg, wheat, peanut)
- 연령대 필터 + 검색어 + 알레르기 필터 조합 지원

### TS-19: 다중 냉장고 지원 ✅ (2026-05-16)
- 냉장고 선택 드롭다운 + 새 냉장고 생성 버튼 추가
- `currentFridgeId` 변수로 현재 선택된 냉장고 추적
- switchFridge()로 냉장고 변경 시 재료를 다시 불러옴
- openNewFridgeModal()로 새 냉장고 이름 입력 후 생성

### TS-20: PWA 앱스토어 제출 준비 ✅ (2026-05-16)
- manifest.json 정보 보강 (description, orientation, lang, dir 추가)
- 아이콘 192x192, 512x512, maskable 512x512 추가
- categories: food, health, lifestyle 등록
- Service Worker는 이미 cache-first + offline fallback 전략 적용됨
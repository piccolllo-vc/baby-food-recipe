# LittleMates 유통기한 알림 시스템 설계

> TS-16: 유통기한 임박 재료 푸시 알림
> 기준: 식약처 식품유형별 소비기한 설정값 (2023~2024)

---

## 1. 식약처 소비기한 데이터 (식재료 60+)

### 채소류
| 재료 | 냉장(일) | 냉동(일) | 알림(일 전) |
|------|----------|----------|-------------|
| 시금치 | 5 | 180 | 1 |
| 버섯 | 5 | 180 | 1 |
| 두부 | 5 | 180 | 1 |
| 오이 | 7 | 0 | 2 |
| 호박 | 7 | 180 | 2 |
| 토마토 | 7 | 180 | 2 |
| 브로콜리 | 7 | 365 | 2 |
| 양배추 | 14 | 180 | 3 |
| 대파 | 7 | 180 | 2 |
| 당근 | 30 | 365 | 5 |
| 감자 | 30 | 365 | 5 |
| 양파 | 30 | 180 | 5 |
| 마늘 | 90 | 365 | 7 |
| 단호박 | 30 | 180 | 3 |

### 과일류
| 재료 | 냉장(일) | 냉동(일) | 알림(일 전) |
|------|----------|----------|-------------|
| 바나나 | 5 | 0 | 1 |
| 아보카도 | 5 | 0 | 1 |
| 포도 | 7 | 180 | 2 |
| 복숭아 | 7 | 180 | 2 |
| 오렌지 | 14 | 180 | 3 |
| 배 | 14 | 180 | 3 |
| 사과 | 30 | 180 | 5 |

### 육류 (고위험)
| 재료 | 냉장(일) | 냉동(일) | 알림(일 전) |
|------|----------|----------|-------------|
| 생선 | 2 | 90 | 1 |
| 연어 | 2 | 90 | 1 |
| 닭고기 | 3 | 180 | 1 |
| 새우 | 3 | 180 | 1 |
| 돼지고기 | 3 | 180 | 1 |
| 오리고기 | 5 | 180 | 1 |
| 소고기 | 5 | 180 | 1 |

### 유제품
| 재료 | 냉장(일) | 냉동(일) | 알림(일 전) |
|------|----------|----------|-------------|
| 두부 | 5 | 180 | 1 |
| 요거트 | 14 | 180 | 2 |
| 우유 | 14 | 180 | 2 |
| 크림치즈 | 14 | 180 | 2 |
| 버터 | 90 | 365 | 7 |
| 치즈 | 180 | 365 | 14 |

### 곡물/가공식품
| 재료 | 냉장(일) | 냉동(일) | 알림(일 전) |
|------|----------|----------|-------------|
| 식빵 | 7 | 90 | 2 |
| 어묵 | 14 | 90 | 2 |
| 만두 | 14 | 180 | 2 |
| 햄 | 14 | 180 | 2 |
| 소시지 | 14 | 180 | 2 |
| 달걀 | 14 | 180 | 3 |
| 김치 | 30 | 180 | 5 |

---

## 2. 유통기한 알림 시스템 설계

### 2.1 데이터 흐름
```
재료 추가 (click)
    ↓
ingredients.json에서 shelfLifeFridge 조회
    ↓
오늘 날짜 + shelfLifeFridge = expiry_date 자동 계산
    ↓
 fridge_items 테이블에 저장 (supabase)
    ↓
앱 시작 시 / 주기적으로 만료 대상 조회
    ↓
유통기한 ≤ warningDays → 알림 발송
```

### 2.2 핵심 로직
```javascript
// 1. 재료 추가 시 자동 계산
function addFridgeItem(ingredientId, category, storageType = 'fridge') {
  const ingredient = INGREDIENTS[category].find(i => i.id === ingredientId);
  
  const shelfLife = storageType === 'fridge' 
    ? ingredient.shelfLifeFridge 
    : ingredient.shelfLifeFreezer;
  
  const expiryDate = new Date();
  expiryDate.setDate(expiryDate.getDate() + shelfLife);
  
  // supabase에 저장
  sb.from('fridge_items').insert({
    name: ingredient.name,
    category: category,
    ingredient_id: ingredientId,
    expiry_date: expiryDate.toISOString().split('T')[0],
    storage_type: storageType,
    warning_days: ingredient.warningDays,
    is_high_risk: ingredient.isHighRisk || false
  });
}

// 2. 알림 체크 (앱 시작 시 실행)
async function checkExpiryNotifications() {
  const { data: items } = await sb.from('fridge_items')
    .select('*')
    .eq('user_id', sb.auth.user().id);
  
  const today = new Date();
  const warningItems = items.filter(item => {
    const expiry = new Date(item.expiry_date);
    const daysLeft = Math.ceil((expiry - today) / (1000 * 60 * 60 * 24));
    return daysLeft <= item.warning_days && daysLeft >= 0;
  });
  
  if (warningItems.length > 0) {
    showExpiryToast(warningItems);
  }
}

// 3. 토스트/배너 UI
function showExpiryToast(items) {
  const messages = items.map(i => `${i.name}: ${getDaysLeft(i.expiry_date)}일 남음`);
  // 예: "닭고기: 1일 남음, 두부: 1일 남음"
  showBanner(`유통기한 임박! ${messages.join(', ')}`, 'warning');
}
```

### 2.3 Supabase 테이블 변경
```sql
ALTER TABLE fridge_items ADD COLUMN IF NOT EXISTS ingredient_id TEXT;
ALTER TABLE fridge_items ADD COLUMN IF NOT EXISTS expiry_date DATE;
ALTER TABLE fridge_items ADD COLUMN IF NOT EXISTS warning_days INTEGER DEFAULT 2;
ALTER TABLE fridge_items ADD COLUMN IF NOT EXISTS is_high_risk BOOLEAN DEFAULT false;
ALTER TABLE fridge_items ADD COLUMN IF NOT EXISTS storage_type TEXT DEFAULT 'fridge';
```

### 2.4 Notification API (앱 사용 중)
```javascript
async function requestNotificationPermission() {
  if ('Notification' in window) {
    const permission = await Notification.requestPermission();
    return permission === 'granted';
  }
  return false;
}

function showBrowserNotification(title, body, icon) {
  if (Notification.permission === 'granted') {
    new Notification(title, { body, icon });
  }
}
```

---

## 3. 기술 구현 옵션

### 옵션 A: 순수前端 (앱 사용 중만 알림)
- ✅ 구현简单
- ❌ 앱 닫으면 알림 못 받음
- 비용: 0원

### 옵션 B: Firebase Cloud Messaging (FCM) 연동
- ✅ 앱 꺼도 알림 받음
- ❌ FCM 설정 필요
- 비용: 무료 (무제한)
- 필요物: firebase-messaging-sw.js

### 옵션 C: Supabase Realtime + Service Worker
- ✅ 기존 인프라 활용
- ❌ 복잡한 설정
- 비용: 0원

---

## 4. 구현 우선순위

| 단계 | 내용 | 난이도 |
|------|------|--------|
| 1단계 | ingredients.json 기반 자동 계산 | 하 |
| 2단계 | expiry_date 컬럼 추가 (migration) | 하 |
| 3단계 | 앱 시작 시 만료 체크 → 토스트 | 하 |
| 4단계 | Browser Notification API | 하 |
| 5단계 (optional) | FCM 푸시 연동 | 중 |

---

## 5. 미확인 재료 처리

```javascript
// ingredients.json의 재료추가옵션
"재료추가옵션": {
  "직접입력": {
    "shelfLifeFridge": 5,  // 기본값 5일
    "shelfLifeFreezer": 90,
    "warningDays": 2,
    "note": "알 수 없는 재료는 기본값 5일"
  }
}

// 직접 입력 시
function addUnknownIngredient(name) {
  const defaultExpiry = 5; // 기본 5일
  
  sb.from('fridge_items').insert({
    name: name,
    category: '직접입력',
    expiry_date: addDays(new Date(), defaultExpiry),
    warning_days: 2,
    is_high_risk: false
  });
}
```

---

*설계일: 2026-05-20*
*식약처 기준: 식품의약품안전처 식품안전나라 (2024 고시)*